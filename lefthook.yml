pre-commit:
  parallel: true
  commands:
    cargo-fmt:
      glob: "*.rs"
      run: cargo fmt -- --check
    clippy:
      glob: "*.rs"
      run: cargo clippy --release --workspace -- -D warnings
    nix-fmt:
      glob: "*.nix"
      run: nix fmt -- --fail-on-change .
